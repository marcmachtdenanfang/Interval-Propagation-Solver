package org.mcnip.solver.Parser;

public interface AST {
    // classes maybe should have a contractor field, 
    // so that the contractor can be called easily
    // NO! After parsing we convert everything to Pairs, Bounds and Triplets!
}
